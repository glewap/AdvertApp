<%= form_for(advert) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :price %>
  <%= f.number_field(:price, step: 0.01) %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :category %>
  <%= f.select(:category) do %>
    

  <br />

  <%= f.submit "Zapisz" %>

<% end %>

